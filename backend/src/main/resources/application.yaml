spring:
  datasource:
    url:  
    username:  
    password:  
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  error:
    whitelabel:
      enabled: false
  port: 5000

jwt:
  signing-key: thisisaverylognstringforsecuritypurposeonly
  expiration-in-ms: 1800000